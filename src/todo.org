* TODO Rendering system
*** TODO Remove per‑frame malloc/realloc in batching – use pre‑allocated pools.

*** Stop orphaning instance VBO every draw – use glBufferSubData or persistent

*** Add renderShutdown() to free batch instance

*** Add rotation support to Quad (mat4x4_rotate_

*** Fix renderAABB to work without breaking batched state – draw after batching or as line

*** Add layer/Z‑ordering (bullets behind enemies, player on

*** Implement persistent mapped buffer for high bullet counts (

*** Add circle rendering (quad + fragment shader discard or triangle

*** Add camera/view transform (follow player,

*** Add simple text output (bitmap font via instanced quads) for debug

*** Add frustum culling to skip off‑screen

*** Integrate particle system (colored quads, short lifetime) using same batching.




* TODO Window events

** TODO Handle Resize

** DONE Store updated width/height in a global or struct WindowDimensions (why? idk yet really maybe ill be able to use it later on something)(done).

** DONE Trigger a renderer update (recalculate projection, viewport, etc.).

** DONE Handle Focus / Minimize (what abt borderless too)

** TODO [#B] Capture WM_ACTIVATEAPP / SDL_EVENT_WINDOW_FOCUS_GAINED / LOST (to be done).

** TODO [#C] Pause the game/editor loop if minimized or unfocused (important) (to be done).

** TODO Handle Close and saving of game data (Dont have a game yet)

** TODO fix the hitbox rendering issues



** TODO Input
keyboard events like wasdown and isdown should properly be handled and logged
mouse inputs cuz its a bullet hell duh
gamepad, also make sure to handle hotplugging properly



* TODO Bullet counts
**Render instancing for bullet meshes. one draw call for atleast 100 bullets
**Pool Allocate some memory for said bullets, dont delete them unless the stage is finished 


